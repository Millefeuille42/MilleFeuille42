apiVersion: 1

datasources:
- name: InfluxDB
  type: influxdb
  url: http://influxdb:8086
  database: telegraf
  user: telegraf
  isDefault: true
  editable: true
  secureJsonData:
      password: password
  jsonData:
      sslmode: "disable"
      httpMode: GET
