apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin
  labels:
    app: ft_services
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      protocol: TCP
      name: http-phpmyadmin
      targetPort: 80
      nodePort: 30500
  selector:
    run: phpmyadmin

